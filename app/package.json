{
  "name": "mixer-sprint",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build:worker": "node build-worker.mjs",
    "deploy": "node build-worker.mjs && wrangler deploy"
  },
  "dependencies": {
    "express": "^4.21.0",
    "dotenv": "^16.4.5"
  }
}
